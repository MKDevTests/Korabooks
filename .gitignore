*.iml
.gradle
/local.properties
/.idea
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
build/
/captures
.externalNativeBuild
.cxx
/output
/.kotlin
/cmake/build*

# Tooling / AI assistants
.github/
.gemini/
.claude/
.beans/
.beans.yml
CLAUDE.md

# Design / working files
New UI/

# Pre-existing vendored third-party repos
third_party/secret-service/

# Prebuilt native libraries
**/androidMain/jniLibs/**/*.so
.worktrees

# Personal migration artifacts
sipurraP-data.tar
*.b64
v2.sqlite
stable.sqlite

# Similar-series bench: local scratch index pulled from the server
scripts/similar-bench/index_*.json
scripts/similar-bench/__pycache__/

# Release notes: written for `gh release`, which keeps them once published.
# Untracked they piled up at the repo root and preflight — rightly — refused
# to tag a release with a dirty tree because of them.
release-notes-*.md
