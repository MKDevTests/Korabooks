<configuration
        xmlns="https://tony19.github.io/logback-android/xml"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="https://tony19.github.io/logback-android/xml https://cdn.jsdelivr.net/gh/tony19/logback-android/logback.xsd"
>
    <appender name="logcat" class="ch.qos.logback.classic.android.LogcatAppender">
        <tagEncoder>
            <pattern>%logger{12}</pattern>
        </tagEncoder>
        <encoder>
            <pattern>[%-20thread] %msg</pattern>
        </encoder>
    </appender>

    <appender name="file" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/komelia.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.FixedWindowRollingPolicy">
            <fileNamePattern>${LOG_DIR}/komelia.%i.log</fileNamePattern>
            <minIndex>1</minIndex>
            <maxIndex>3</maxIndex>
        </rollingPolicy>
        <triggeringPolicy class="ch.qos.logback.core.rolling.SizeBasedTriggeringPolicy">
            <!-- Per-file ceiling; total budget = this x (maxIndex + 1).
                 Set from the user's log-size cap via the LOG_MAX_FILE_SIZE
                 system property in App.initLogging(); default keeps the old 5MB. -->
            <maxFileSize>${LOG_MAX_FILE_SIZE:-5mb}</maxFileSize>
        </triggeringPolicy>
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%-20thread] %-5level %logger{20} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Level set from App.initLogging() via the LOG_LEVEL system property:
         DEBUG for debug builds, INFO for release. Default INFO is the safe
         fallback if the property is somehow unset. -->
    <!-- The file appender writes on the calling thread, so every log statement
         cost a disk write wherever it was made — including the reader's
         per-page path. Buffer it: the queue absorbs bursts, and when it fills
         AsyncAppender drops TRACE/DEBUG/INFO first, never WARN or ERROR. -->
    <appender name="asyncFile" class="ch.qos.logback.classic.AsyncAppender">
        <queueSize>512</queueSize>
        <!-- Never block the app thread waiting for the log queue. -->
        <neverBlock>true</neverBlock>
        <appender-ref ref="file" />
    </appender>

    <!-- Exposed logs the full text of every statement it runs. Mirroring a
         catalogue is a hundred and forty thousand of them, each one a string
         built, formatted and pushed to logcat before the insert it describes
         even happens. On a debug build that logging costs more than the writes
         it narrates. A failing statement still speaks, at WARN. -->
    <logger name="Exposed" level="WARN" />

    <root level="${LOG_LEVEL:-INFO}">
        <appender-ref ref="logcat" />
        <appender-ref ref="asyncFile" />
    </root>
</configuration>
